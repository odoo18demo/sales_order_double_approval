<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- CRM TEAM FORM -->
    <record id="team_crm_view_inherit" model="ir.ui.view">
        <field name="name">crm.team.form.inherit.approval</field>
        <field name="model">crm.team</field>
        <field name="inherit_id" ref="sales_team.crm_team_view_form"/>
        <field name="arch" type="xml">

            <!-- Rename Team Leader -> Revisor -->
            <xpath expr="//field[@name='user_id']" position="attributes">
                <attribute name="string">Revisor</attribute>
                <attribute name="domain">[('id', 'in', member_ids)]</attribute>
            </xpath>

            <!-- Add Manager -->
            <xpath expr="//field[@name='user_id']" position="after">

                <field name="sales_manager_ids" invisible="1"/>

                <field name="second_approval_id"
                       string="Manager"
                       domain="[('id', 'in', sales_manager_ids)]"/>

            </xpath>

        </field>
    </record>


    <!-- CRM TEAM TREE -->
    <record id="team_crm_list_view_inherit" model="ir.ui.view">
        <field name="name">crm.team.tree.inherit.approval</field>
        <field name="model">crm.team</field>
        <field name="inherit_id" ref="sales_team.crm_team_view_tree"/>
        <field name="arch" type="xml">

            <xpath expr="//field[@name='user_id']" position="attributes">
                <attribute name="string">Revisor</attribute>
            </xpath>

            <xpath expr="//field[@name='user_id']" position="after">

                <field name="sales_manager_ids"
                       column_invisible="1"/>

                <field name="second_approval_id"
                       string="Manager"/>

            </xpath>

        </field>
    </record>

</odoo>